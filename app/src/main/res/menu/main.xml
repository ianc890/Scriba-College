<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/bluetooth"
        android:title="@string/connect_scriba"
        app:showAsAction="ifRoom"
        android:icon="@mipmap/ic_bluetooth_white_24dp"
        android:onClick="bluetooth"/>

    <item
        android:id="@+id/notes"
        android:title="View Notes"
        app:showAsAction="ifRoom"
        android:icon="@android:drawable/ic_menu_view"
        android:onClick="viewNotes"/>

    <item
        android:id="@+id/quizQuestion"
        android:title="Create Quiz Questions"
        app:showAsAction="never"
        android:onClick="createQuestion"/>

    <item
        android:id="@+id/quiz"
        android:title="Take Quiz"
        app:showAsAction="never"
        android:onClick="takeQuiz"/>
</menu>
